export type ListProps = {
    selectedValue: string
}

export type Repository = {
    name: string;
    full_name: string;
    created_at: string;
    stargazers_count: number;
    language: string;
    html_url: string;
}
